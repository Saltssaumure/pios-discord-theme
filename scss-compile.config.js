/** @type {import('bd-scss/lib/config').Config} */
export default {
    meta: {
        name: "piOS" /* The official name of your theme. */,
        scss: "pios" /* The name of your base scss file. */,
        repo: "pios-discord-theme" /* The name of your theme repository. */,
        version: "dev" /* The version of your theme. */
    }
};
